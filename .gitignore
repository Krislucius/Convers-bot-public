# Dependencies and caches
node_modules/
.node_modules.lock
.tanstack/
.vercel/
dist/
build/
.nitro/
.output/

# Secrets and local env
.env
.env.local
.env.*.local
*.pem
*.key

# Operator-only notes (not required for build/runtime)
.grok/project_memory.md
artifacts/ACCESS.md

# Logs / OS
*.log
.grok/preview.log
.grok/preview.pid
.grok/status
.DS_Store
Thumbs.db

# Platform runtime (not application source)
.grok/skills/
.grok/references/

# QA / session noise
screenshots/
# Sandbox PGLite cluster (account projects/sessions; not source)
artifacts/pglite/
# Sandbox Better Auth signing secret (pairs with durable PGLite)
artifacts/grok-auth-preview-secret
attachments/
artifacts/browsed_files/
artifacts/workspace-source/

# SUPERSEDED Python prototype
conversation-bot/

# Python junk if any
**/__pycache__/
*.pyc
.pytest_cache/

# Export archives (source is the repo)
*.zip

# Editor / patch debris
.idea/
.vscode/
*.orig
*.rej
*~
